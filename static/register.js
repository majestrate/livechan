//
// board registration
//


// get the name of the board we are on
getBoardName = function() {
  var idx = location.pathname.indexOf("/", 1);
  return location.pathname.substr(idx+1);
}

// begin register process
register = function() {
  
}
